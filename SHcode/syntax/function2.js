const { setupMaster } = require("cluster");

console.log(Math.round(1.6)); //2 반올림

function sum(first,second){ //parameter variable

    return first+second;

}
console.log(sum(2,4)); //argument  입력값. 인자

Math.round(1.6)
